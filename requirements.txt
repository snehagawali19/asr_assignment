requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.0.0
jiwer>=3.0.0
rapidfuzz>=3.0.0
indic-transliteration>=2.3.0
matplotlib>=3.7.0
tabulate>=0.9.0

# Optional — only needed if running the FLEURS sanity check (script 06)
datasets>=2.14.0
scipy>=1.10.0
soundfile>=0.12.0

# Recording generator (scripts/generate_recordings.py)
edge-tts>=6.1.0
imageio-ffmpeg>=0.5.0
